import React from "react";
import ReactDOM from "react-dom";
import TodoContainer from "./components/TodoContainer";
import "./App.css";

const element = <TodoContainer />;

ReactDOM.render(element, document.getElementById("root"));
